# PI Request App

## Functional Overview
Used by Purchasing to onboard Suppliers (similar to Coupa but different rules).
*   **Mandatory**: Identifications (PI ID).

## Technical Implementation
*   **Service**: `PIService` (`srv/pi-service.cds`)
*   **Entity**: `PIRequests`.
